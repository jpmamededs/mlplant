"""preprocessing.py — Data cleaning and preprocessing."""

import pandas as pd


def preprocess(df: pd.DataFrame) -> pd.DataFrame:
    """Clean the input dataframe and return the result."""
{{ code | indent(4, first=true) }}
    return df
