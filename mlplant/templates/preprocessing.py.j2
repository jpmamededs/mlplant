"""preprocessing.py — Data cleaning and preprocessing."""

import pandas as pd
from config import *
{% if module_code %}

{{ module_code }}
{% endif %}

def preprocess(df):
    """Apply preprocessing steps and return the result."""
{% if body_code %}
{{ body_code | indent(4, first=true) }}
    return df
{% else %}
    return df
{% endif %}
