"""artifacts.py — Model serialization and loading."""

import joblib

MODEL_PATH = "model.joblib"
{% if module_code %}

{{ module_code }}
{% endif %}

def save_model(model, path: str = MODEL_PATH) -> None:
    """Serialize and persist the trained model."""
{% if body_code %}
{{ body_code | indent(4, first=true) }}
{% else %}
    pass
{% endif %}


def load_model(path: str = MODEL_PATH):
    """Load and return the model from disk."""
    return joblib.load(path)
